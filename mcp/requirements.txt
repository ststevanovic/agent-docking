# Main dependencies
model-context-protocol-sdk>=0.1.0

# Molecular docking dependencies
# The following packages should be installed via conda:
# conda install -c bioconda smina
# conda install conda-forge::openbabel
# conda install conda-forge::chembl_webresource_client

# Python dependencies for molecular operations
numpy>=1.21.0
pandas>=1.3.0
rdkit>=2022.03.1  # for molecular operations
pdb-tools>=2.4.0  # for handling PDB files

# Interface and notebook dependencies
notebook>=7.0.0  # for Jupyter notebook
gradio>=3.50.0  # for UI interface
jupyter>=1.0.0
ipywidgets>=8.0.0

# RAG dependencies
langchain>=0.1.0
langchain-community>=0.0.10
pymongo>=4.6.0
openai>=1.12.0
python-dotenv>=1.0.0
chromadb>=0.4.22  # backup local vector store option

# Optional but recommended
pytest>=6.0.0  # for testing
black>=22.0.0   # for code formatting
flake8>=4.0.0  # for linting
